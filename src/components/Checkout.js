import React from 'react';

const Checkout = (props) => {
    const { cart, token, setAlert } = props;
    
    return(
        <div className='checkout-container'>
            <h1>CHECKOUT</h1>
        </div>
    )
}

export default Checkout;